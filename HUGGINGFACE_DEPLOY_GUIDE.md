# دليلك الكامل لرفع وتشغيل المشروع على Hugging Face Spaces 🚀

الملفات في مشروعك جاهزة تماماً للعمل على Hugging Face، لأن ملف `Dockerfile` مهيأ ليعمل على بورت `7860` المفضل لديهم. لاتمام الرفع والتشغيل بنجاح، اتبع الخطوات البسيطة التالية:

## الخطوة 1: إنشاء Space جديد على Hugging Face
1. قم بزيارة موقع [Hugging Face](https://huggingface.co) وسجل الدخول بحسابك.
2. من القائمة العلوية أو من صفحتك الشخصية، اضغط على **New Space**.
3. في صفحة إنشاء الـ Space:
   - **Space name**: اكتب اسم المشروع (مثلاً `final-plant`).
   - **License**: يمكنك اختيار `MIT` أو تركها كما هي.
   - **Select the Space SDK**: هذه هي الخطوة الأهم! اختر **Docker** ثم اختر **Blank**.
   - **Space hardware**: اختر `Free` أو الباقة المناسبة لك.
   - اضغط على الزر الأسفل **Create Space**.

## الخطوة 2: رفع ملفات المشروع للـ Space
أمامك طريقتان (اختر الأسهل لك):

### الطريقة الأولى (الرفع يدوياً من المتصفح الأسهل):
1. داخل الـ Space الجديد، اذهب إلى تبويب **Files**.
2. اضغط على **Add file** ثم **Upload files**.
3. قم بتحديد *كل* الملفات والمجلدات الموجودة في مجلد مشروعك (`app.py`, `api.py`, `Dockerfile`, `requirements.txt`, ومجلدات `application`, `domain`, `infrastructure`, وغيرها) وارفعها كلها.
4. في الأسفل اكتب رسالة للحفظ واضغط **Commit changes to main**.

### الطريقة الثانية (باستخدام Git):
إذا كنت تفضل أو تستخدم لوحة الأوامر (Terminal)، استخدم أوامر الجيت التي تظهر لك أسفل كلمة `Clone repository` في الصفحة الرئيسية للـ Space الخاص بك.

## الخطوة 3: إضافة مفاتيح الـ API (أهم خطوة ⚠️)
كما ذكرنا سابقاً، منصة Hugging Face لا تقرأ ملف `.env` الخفي الموجود على جهازك. عليك إدخال المفاتيح يدوياً لحماية بياناتك:
1. في الـ Space الخاص بك، اذهب إلى تبويب **Settings** (الإعدادات).
2. انزل للأسفل إلى قسم **Variables and secrets**.
3. اضغط على زر **New secret**، وقم بإضافة كل المتغيرات الموجودة في ملف `.env` الخاص بك. على سبيل المثال:
   - **Name:** `GROQ_API_KEY`  👈 **Value:** `مفتاحك_الخاص_بـ_Groq`
   - **Name:** `GROQ_VISION_MODEL` 👈 **Value:** `meta-llama/llama-4-scout-17b-16e-instruct`
   - وأي مفاتيح أخرى تمتلكها في `.env` مثل `TAVILY_API_KEY` أو `PLANTNET_API_KEY` (إذا كنت مسجلاً فيهم).

*(ملاحظة: تأكد أنه Secret وليس Variable عادي حتى لا يرى زوار موقعك الخاص مفاتيحك السرية).*

## الخطوة 4: الانطلاق والبناء التلقائي ⏳
بمجرد أن تقوم بحفظ الملفات والـ Secrets، ستقوم منصة Hugging Face تلقائياً بعمل ما يسمى **Building** (بناء بيئة العمل الخاصة بك عن طريق الـ `Dockerfile`).
1. ستلاحظ دائرة صفراء مكتوباً بجانبها Building أو Starting.
2. يمكن الضغط على كلمة **Logs** لكي تشاهد سيرفرك وهو يقوم بتثبيت المكتبات (requirements.txt).
3. العملية قد تستغرق من دقيقة لثلاث دقائق.
4. عند الانتهاء، ستتحول الدائرة إلى اللون الأخضر ومكتوباً بجانبها **Running 🟢**.

مبروك! 🎉 مشروعك الآن يعمل بالكامل، ويمكنك أخذ رابط الـ Space (مثل: `https://huggingface.co/spaces/YourUsername/final-plant`) وربطه مع الـ Frontend الخاص بك بشكل مباشر أو مشاركته مع أي شخص.